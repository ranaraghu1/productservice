springfox-swagger version 3.0.0 and spring boot starter parent version 2.6.0 issues

resolved
Use springfox-swagger version 2.9.2 and spring boot starter version 2.5.6

http://localhost:8080/productapi/swagger-ui.html
http://localhost:8080/productapi/v2/api-docs

for swagger configuration

pom.xml

springfox-swagger2
springfox-swagger-ui

@SpringBootApplication
@EnableSwagger2
public class HellospringbootApplication {

	public static void main(String[] args) {
		SpringApplication.run(HellospringbootApplication.class, args);
	}